// CREATE <div> box in Render method
// ADD color to box using state in constructor method
// WHEN box is clicked color should change
// To achieve that create onClick handle event and assign it to colorChange function
// In colorChange method, use setState to change the color
// for clickCounter, initialize it in State and increment it under colorChange
// box color should toggle back to its original color
